2015-03-02
原本想按照格子生成地图。
但是地图的单个元素可能是多格子的，所以不能这样进行更新。
所以决定用坐标来生成地图。
2015-03-03
需要加入一个数据读取的代码，类似xml这种
2015-03-12
缩放是中心缩放的，所以当缩放之后，需要对坐标进行便宜。如果不知道是如何缩放的，可以把debug模式放开。可以看到未缩放的效果。(前提是boundbox没有进行缩放) ( 缩放比 - 1 ) / 2
有时候读的数据放在xml多的话，写得也同样变多。
2015-03-14
as3的xml构造起来非常困难，问mac保存构造xml的方式。他给了解决方案也不是很适合，可能是API的关系。但是他提供了一个bytearray的方法，可以直接把二进制数据保存/读取。
2015-03-15
需要registerClassAlias进行类型绑定，并且包含包信息在里面registerClassAlias("gamemap.GameMapElementInfo", GameMapElementInfo);否则传出的类型识别不出。
2015-03-18
更新了java1.8.然后flashdeveloper就不能用了。需要卸载完整后重装1.7才能使用
2015-04-08
添加了重力器的图像加载
想要设计一种建筑元件，从上面掉下来时候会被接住，从下面飞上去时，则不会阻挡飞上去的路径
2015-04-09
flx里面的FlxG.keys.getKeyCode只支持大写字符串字母