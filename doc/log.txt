我自己的子弹发射在某些角度发射会没有很平均的角度差别。但是传递了角度差别的发射倒是对的。计算方式可能还是有问题

原本以为平方/平方可以得到正确的cosin值。后来发现不对

Math.sin的参数是radians

按照起始点和结束点进行发射会有一个问题。就是当结束点离起始点很进的时候。就会出现计算错误。

有个gravity的build在那里。所以导致会碰撞产生

计算霰弹的子弹射程还是有问题

当象限是++，--的时候。对一个角度进行+角度再算出sin，cos的时候是正确的。
当象限是-+,+-的时候。对一个角度进行-角度在进行cos才是正确的。
而且只能针对cos进行角度变更处理
否则射程就会变短。但是为什麽呢？